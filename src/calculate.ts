const calculate = (number: number) => {
  return number * number;
};

/**
 * 
 * Что плохо:
 * 
 * Название функции слишком обширное и не отражает реальное применение
 * Запись можно сделать короче
 * 
 */

/**
 * 
 * Улучшения:
 * 
 * Изменено наименование функции, теперь лучше отражает её суть
 * Ипсользована более короткая запись функции
 * 
 */

const square = (n: number) => n * n;